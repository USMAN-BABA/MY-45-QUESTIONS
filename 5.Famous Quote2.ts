let favouritePerson: string = "Albert Einstein";

let message: string = '"A person who never made a mistake never tried anything new."';

console.log(`${favouritePerson} once said, ${message}`);
