"use strict";
let userNames = ["Sufyan", "Asharib", "Faham", "Fawad", "SanaUllah"];
if (userNames.length === 0) {
    console.log("We need to find some users!");
}
else {
    userNames = [];
    console.log(`No users in the Array : ${userNames.length}.`);
}
